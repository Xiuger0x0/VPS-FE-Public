# CI/CD 環境變數配置
# 這個文件用於 CI/CD 流程，包含可以公開的環境變數

VITE_APP_NAME='Xiuger'
VITE_SHOW_DEV_LINK=false

# LINE OAuth 配置 (只包含前端需要的公開資訊)
VITE_LINE_CLIENT_ID="${LINE_CLIENT_ID}"
VITE_REDIRECT_URI="${REDIRECT_URI}"

# API 配置
VITE_API_BASE_URL="${API_BASE_URL}"
